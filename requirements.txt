# QuantAI Unified — requirements.txt
yml>=0.0.1 

# === Dados de mercado ===
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=16.0.0        # Parquet (neuralops777)

# === Machine Learning ===
scikit-learn>=1.4.0    # Random Forest + Linear Baseline
xgboost>=2.0.0         # XGBoost preditivo (neuralops777)

# === Otimização de portfólio ===
PyPortfolioOpt>=1.5.5  # Ledoit-Wolf + L2 (neuralops777)
scipy>=1.12.0

# === IA Generativa ===
openai>=1.30.0         # OpenAI Agents SDK (Gabriel)
langchain>=0.2.0       # Orquestração LLM (neuralops777)
langchain-openai>=0.1.8

# === Dashboard ===
streamlit>=1.35.0      # Interface web interativa (Gabriel)
plotly>=5.18.0         # Gráficos interativos

# === Relatório PDF ===
reportlab>=4.2.0       # PDF profissional (versão unificada)
fpdf2>=2.7.8           # Fallback PDF

# === Configuração ===
pyyaml>=6.0.1          # Leitura de configs YAML
python-dotenv>=1.0.0   # Variáveis de ambiente

# === Dados macro BCB ===
requests>=2.32.0       # API SGS/BCB (neuralops777)

# === Testes ===
pytest>=8.2.0
pytest-cov>=5.0.0

# === Utilitários ===
matplotlib>=3.8.0
seaborn>=0.13.0
tabulate>=0.9.0